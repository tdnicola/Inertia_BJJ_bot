module.exports = {
    name: "Positions",
    async execute(message) {
        await message.author.send(`Major positions:
          Closed Guard
          Half Gauard
          Open Guard
          Turtle
          Mount
          Side Control
          Back
      `);
    },
};


